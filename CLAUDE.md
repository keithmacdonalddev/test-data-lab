@AGENT_WORKFLOW.md
@AGENTS.md

# Claude Code — Test Data Lab public pages

Use the imported shared project rules. This static site has the approved optional shared coding-agent hooks and three core roles; Node is used only by the development harness. Do not confuse it with the sibling `qbo` application.
